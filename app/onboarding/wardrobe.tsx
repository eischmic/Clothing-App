import React, { useState } from 'react'; import { ScrollView, Text } from 'react-native'; import { router } from 'expo-router'; import { SafeAreaView } from 'react-native-safe-area-context'; import { ImagePickerGrid } from '@/components/ImagePickerGrid'; import { OnboardingProgress } from '@/components/OnboardingProgress'; import { PrimaryButton } from '@/components/primitives'; import { useTheme } from '@/theme/useTheme';
export default function WardrobeOnboarding() { const { base, type, spacing } = useTheme(); const [uris, setUris] = useState<string[]>([]); return <SafeAreaView style={{ flex: 1, backgroundColor: base.canvas }}><OnboardingProgress step={3} /><ScrollView contentContainerStyle={{ padding: spacing.lg, gap: spacing.md }}><Text style={[type.display, { color: base.textHi }]}>What do you already own?</Text><Text style={[type.body, { color: base.textMid }]}>Add 5–10 pieces. We’ll find clothes that work with them instead of duplicating them.</Text><ImagePickerGrid uris={uris} max={10} onAdd={(items) => setUris((old) => [...old, ...items])} onRemove={(uri) => setUris((old) => old.filter((x) => x !== uri))} /><PrimaryButton label={uris.length ? 'Continue' : 'Skip for now'} onPress={() => router.push('/onboarding/analyzing' as never)} variant={uris.length ? 'solid' : 'ghost'} /></ScrollView></SafeAreaView>; }
