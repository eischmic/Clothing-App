import React, { useEffect, useState } from 'react'; import { ScrollView, Text, View } from 'react-native'; import { router } from 'expo-router'; import { SafeAreaView } from 'react-native-safe-area-context'; import { OnboardingProgress } from '@/components/OnboardingProgress'; import { PaletteRow } from '@/components/PaletteRow'; import { RadarChart } from '@/components/RadarChart'; import { PrimaryButton } from '@/components/primitives'; import { buildStyleProfile } from '@/lib/profile'; import { useAppStore } from '@/store/useAppStore'; import { useTheme } from '@/theme/useTheme';
const stages = ['Reading your inspiration', 'Finding your palette', 'Mapping your style'];
export default function Analyzing() { const { base, accent, type, spacing } = useTheme(); const questionnaire = useAppStore((s) => s.questionnaire); const images = useAppStore((s) => s.inspoImages); const setProfile = useAppStore((s) => s.setStyleProfile); const [stage, setStage] = useState(0); const [done, setDone] = useState(false); const profile = buildStyleProfile({ attributes: images.flatMap((x) => x.attributes ? [x.attributes] : []), questionnaire }); useEffect(() => { const timers = [setTimeout(() => setStage(1), 500), setTimeout(() => setStage(2), 1000), setTimeout(() => { setProfile(profile); setDone(true); }, 1500)]; return () => timers.forEach(clearTimeout); }, []); return <SafeAreaView style={{ flex: 1, backgroundColor: base.canvas }}><OnboardingProgress step={4} /><ScrollView contentContainerStyle={{ padding: spacing.lg, gap: spacing.lg, flexGrow: 1, justifyContent: 'center' }}>{!done ? <View style={{ gap: spacing.md }}>{stages.map((name, index) => <Text key={name} style={[type.title, { color: index <= stage ? accent.bright : base.textLow }]}>{index <= stage ? '✓ ' : '○ '}{name}</Text>)}</View> : <View style={{ gap: spacing.lg }}><Text style={[type.display, { color: base.textHi }]}>This is your style.</Text><Text style={[type.title, { color: accent.bright }]}>{profile.tags.join(' · ')}</Text><PaletteRow colors={profile.dominantColors} /><RadarChart vector={profile.vector} /><PrimaryButton label="See your style" onPress={() => router.replace('/(tabs)' as never)} /></View>}</ScrollView></SafeAreaView>; }
