import React from 'react'; import { Text, View } from 'react-native'; import type { Category } from '@/lib/types'; import { useTheme } from '@/theme/useTheme';
export function CoverageBars({ coverage }: { coverage: Record<Category, { count: number; target: number }> }) { const { base, accent, type, spacing } = useTheme(); return <View style={{ gap: spacing.sm }}>{(Object.keys(coverage) as Category[]).map((category) => { const x = coverage[category]; const p = Math.min(1, x.count / x.target); return <View key={category}><View style={{ flexDirection: 'row', justifyContent: 'space-between' }}><Text style={[type.caption, { color: base.textMid }]}>{category}</Text><Text style={[type.caption, { color: base.textLow }]}>{x.count}/{x.target}</Text></View><View style={{ height: 5, backgroundColor: base.hairline, borderRadius: 3, marginTop: 3 }}><View style={{ width: `${p * 100}%`, height: 5, borderRadius: 3, backgroundColor: p === 1 ? accent.base : base.textLow }} /></View></View>; })}</View>; }
