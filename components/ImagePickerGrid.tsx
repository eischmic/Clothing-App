import React, { useState } from 'react';
import { Image, Pressable, Text, View } from 'react-native';
import * as ImagePicker from 'expo-image-picker';
import { useTheme } from '@/theme/useTheme';
export function ImagePickerGrid({ uris, onAdd, onRemove, max }: { uris: string[]; onAdd: (uris: string[]) => void; onRemove: (uri: string) => void; max: number }) { const { base, accent, radii, spacing, type } = useTheme(); const [error, setError] = useState(''); const add = async () => { setError(''); const permission = await ImagePicker.requestMediaLibraryPermissionsAsync(); if (!permission.granted) { setError('Photo access is needed to add images.'); return; } const result = await ImagePicker.launchImageLibraryAsync({ mediaTypes: ['images'], allowsMultipleSelection: true, selectionLimit: max - uris.length, quality: .7, base64: false }); if (!result.canceled) onAdd(result.assets.slice(0, max - uris.length).map((asset) => asset.uri)); }; return <View style={{ gap: spacing.sm }}><View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: spacing.sm }}>{uris.map((uri) => <View key={uri} style={{ width: 88, height: 88 }}><Image source={{ uri }} style={{ width: 88, height: 88, borderRadius: radii.tile }} /><Pressable onPress={() => onRemove(uri)} accessibilityRole="button" accessibilityLabel="Remove image" style={{ position: 'absolute', top: 4, right: 4, width: 24, height: 24, borderRadius: 12, backgroundColor: base.canvas, alignItems: 'center', justifyContent: 'center' }}><Text style={{ color: base.textHi }}>×</Text></Pressable></View>)}{uris.length < max ? <Pressable onPress={add} accessibilityRole="button" accessibilityLabel="Add images" style={{ width: 88, height: 88, borderRadius: radii.tile, borderStyle: 'dashed', borderWidth: 1, borderColor: accent.dim, alignItems: 'center', justifyContent: 'center' }}><Text style={[type.title, { color: accent.bright }]}>＋</Text></Pressable> : null}</View>{error ? <Text style={[type.caption, { color: base.danger }]}>{error}</Text> : null}</View>; }
