import React from 'react';
import { Pressable, Text, View } from 'react-native';
import type { Recommendation } from '@/lib/types';
import { GarmentArt } from '@/components/GarmentArt';
import { ScoreRing, Surface } from '@/components/primitives';
import { useTheme } from '@/theme/useTheme';
import { useAppStore } from '@/store/useAppStore';
export function ProductCard({ recommendation, onPress }: { recommendation: Recommendation; onPress?: () => void }) { const { product } = recommendation; const { base, accent, spacing, type } = useTheme(); const saved = useAppStore((s) => s.savedProductIds.includes(product.id)); const toggle = useAppStore((s) => s.toggleSaved); return <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={`${product.name}, ${product.brand}, ${Math.round(recommendation.total * 100)} percent match`}><Surface level={2} style={{ marginBottom: spacing.sm }}><View style={{ flexDirection: 'row', gap: spacing.sm }}><GarmentArt category={product.category} color={product.color} size={82} /><View style={{ flex: 1, gap: 2 }}><Text numberOfLines={2} style={[type.body, { color: base.textHi, fontWeight: '700' }]}>{product.name}</Text><Text style={[type.caption, { color: base.textMid }]}>{product.brand} · ${product.price}</Text>{recommendation.reasons.slice(0, 2).map((reason) => <Text key={reason} style={[type.caption, { color: accent.bright }]}>{reason}</Text>)}</View><View style={{ alignItems: 'center', gap: 4 }}><ScoreRing value={recommendation.total} size={52} /><Pressable onPress={(e) => { e.stopPropagation(); toggle(product.id); }} accessibilityRole="button" accessibilityLabel={`${saved ? 'Remove' : 'Save'} ${product.name}`}><Text style={[type.title, { color: saved ? accent.base : base.textLow }]}>{saved ? '♥' : '♡'}</Text></Pressable></View></View></Surface></Pressable>; }
