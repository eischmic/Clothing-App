import React from 'react'; import { Image, Pressable, Text, View } from 'react-native'; import type { WardrobeItem } from '@/lib/types'; import { GarmentArt } from '@/components/GarmentArt'; import { useTheme } from '@/theme/useTheme';
export function WardrobeItemCard({ item, onRemove }: { item: WardrobeItem; onRemove: () => void }) { const { base, type, radii } = useTheme(); return <View style={{ width: '48%', gap: 5 }}>{item.uri ? <Image source={{ uri: item.uri }} style={{ width: '100%', aspectRatio: 1, borderRadius: radii.tile }} /> : <GarmentArt category={item.category} color={item.color} size={160} />}<Text numberOfLines={1} style={[type.caption, { color: base.textHi }]}>{item.name}</Text><Pressable onPress={onRemove} accessibilityRole="button" accessibilityLabel={`Remove ${item.name}`}><Text style={[type.caption, { color: base.danger }]}>Remove</Text></Pressable></View>; }
